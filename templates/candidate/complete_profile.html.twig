{% extends 'base.html.twig' %}

{% block title %}RH Conseil - Compléter le profil{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>Compléter le profil de candidat</h1>
    {{ form_start(candidate_form) }}
        {{ form_row(candidate_form.civility) }}
        {{ form_row(candidate_form.lastname) }}
        {{ form_row(candidate_form.firstname) }}
        {{ form_row(candidate_form.title) }}
        <button type="submit">Enregistrer</button>
    {{ form_end(candidate_form) }}
</div>
{% endblock %}
